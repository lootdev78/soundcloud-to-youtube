# soundcloud-to-youtube
SoundCloud track → cover-art MP4 → YouTube upload. CLI with per-URL resumable state, retry + exponential backoff, cached OAuth (Termux-friendly), brand-channel guard, and metadata-only updates without re-uploading.
